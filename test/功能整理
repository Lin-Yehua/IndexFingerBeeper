当前的功能可以分为以下几个部分

预启动部分

    开机动画：播放内置的开机动画（如果音频存在则同时播放音频）

    USB设备模拟：当有USB主机事件时进入USB模式，此时ESP解除与FATFS的挂载

    启动状态警告：在littleFS、FatFS为挂载或损坏的时候发出的警告

    更新检测：检测UPdate内部的文件并且进行更新

    备份恢复：当FatFS损坏时恢复出厂设置

功能部分（APP_MOD）
    
    显示模块：负责将文本显示到屏幕（带动画\无回滚\直接显示全部）

    显示封装：基于显示模块，根据传入的参数确定逻辑。包括按键一次取消回滚，按两次显示全部，显示完成后等待直到按键触发、显示完成后等待直到软件触发

    音频部分：分为音频读取，音频混合，以及音频输出

    主逻辑控制：负责控制每个界面的跳转，以及功能中间的连接

    中断控制器：接收来自中断源的请求，并且在合适的时候进行中断的触发
    
软件驱动层：

    功能驱动：包括从FATFS读取CSV，读取WAV的函数在这个文件里面
    无线驱动：从网页读取数据，写入FATFS

    
无线部分：

