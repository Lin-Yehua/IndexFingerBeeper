当前的功能可以分为以下几个部分

预启动部分

    开机动画：播放内置的开机动画（如果音频存在则同时播放音频）

    USB设备模拟：当有USB主机事件时进入USB模式，此时ESP解除与FATFS的挂载

    启动状态警告：在littleFS、FatFS为挂载或损坏的时候发出的警告

    更新检测：检测UPdate内部的文件并且进行更新

    备份恢复：当FatFS损坏时恢复出厂设置

功能部分（APP_MOD）
    
    显示模块：负责将文本显示到屏幕（带动画\无回滚\直接显示全部）

    显示封装：基于显示模块，根据传入的参数确定逻辑。包括按键一次取消回滚，按两次显示全部，显示完成后等待直到按键触发、显示完成后等待直到软件触发

    音频部分：分为音频读取，音频混合，以及音频输出

    主逻辑控制：负责控制每个界面的跳转，以及功能中间的连接

    中断控制器：接收来自中断源的请求，并且在合适的时候进行中断的触发


    
软件驱动层：

    功能驱动：包括从FATFS读取CSV，读取INI，读取WAV的函数在这个文件里面
    
    无线驱动：从网页读取数据，写入FATFS

    USB模拟、磨损均衡：负责电脑与Flash的写入

    联网获取：从服务端读取数据并且解析

无线部分：

    ESP-NOW检测：负责监听ESP-NOW广播，是最高优先级中断源

    网页消息检测：监听网页消息，网页立即消息，网页图片消息（3个部分拆分为3个文件） 

    AP模式：热点启动10分钟，如果连续10分钟没有操作则关闭，直到有操作（按键）后自动唤醒

    联网模式：分为进入模式的启动初始化函数，以及运行部分

    省电模式：提供深度睡眠选项，热点不启动，仅保留被动ESP-NOW监听

文件细分为：

显示驱动
显示封装

音频读取
音频混合
音频驱动

基础csv读写驱动
统一data.csv读写接口:根据序号读取文本/写入对应序号的对应文本 
统一schlude.csv读写接口:根据标签读取数据/写入数据 (枚举变量)


基础ini读写驱动
统一Setting.ini读取接口:根据标签读取数据/根据标签写入值(枚举变量)

